import React from "react"

import Layout from "../components/Layout"

const NotFoundPage = () => (
  <Layout title="Victor's Pics | Not found">
    <pre>not found.</pre>
  </Layout>
)

export default NotFoundPage
