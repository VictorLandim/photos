import React from "react"

import { Layout } from "../components/Layout"

const NotFoundPage = () => (
  <Layout title="not found">
    <h1 className="layout__title layout__title--not-found">
      lost?
    </h1>
  </Layout>
)

export default NotFoundPage
